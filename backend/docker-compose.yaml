version: '3'

services:
    mongodb:
        image: mongo:3.6.3
        container_name: mongodb
        restart: always
        ports:
            - 27017:27017
        volumes:
            - ./data:/data/db
        